# Working assets

Keep original licensed assets here. Copy only render-ready media into `public/media/`.
Both locations are ignored. Record origin, license, consent and intended use in
the video's `projects/<name>/assets.json`. The renderer serves `public/`, never `input/`.
